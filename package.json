{
  "name": "rate_watch",
  "version": "0.3.0",
  "description": "Crypto price watcher",
  "main": "index.js",
  "homepage": "https://jikkujose.github.io/rate_watch",
  "repository": "git@github.com:JikkuJose/rate_watch.git",
  "author": "Jikku Jose <jikkujose@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "polished": "^4.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "styled-components": "^5.2.0",
    "tachyons": "^4.9.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "react-scripts": "4.0.1",
    "size-limit": "^4.9.1"
  },
  "scripts": {
    "start": "NODE_PATH=src/ react-scripts start",
    "build": "NODE_PATH=src/ react-scripts build",
    "test": "NODE_PATH=src/ react-scripts test --env=jsdom",
    "eject": "NODE_PATH=src/ react-scripts eject",
    "deploy": "NODE_PATH=src/ react-scripts build && gh-pages -d build",
    "size": "NODE_PATH=src/ size-limit"
  },
  "size-limit": [
    {
      "path": "build/static/js/main.70721bdc.js",
      "limit": "60 KB"
    }
  ],
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
